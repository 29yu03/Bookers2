<div>
  <%= flash[:notice] %>
</div>

<div class="container px-5">
  <div class="row">
    <%= render 'list', users: @users %>

    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table>
          <thead>
          <tr class="row-md-12 border-top">
            <th class="col-3 p-3"></th>
            <th class="col-3 p-3">Title</th>
            <th class="col-6 p-3 ">Opinion</th>
          </tr>
        </thead>
        <tbody>
            <% @books.each do |book| %>
          <tr class="row-md-12 border-top">
            <td class="col-3 p-3"><%= image_tag @user.get_profile_image(100,100) %></td>
            <td class="col-3 p-3 align-top"><%= link_to book.title, book %></td>
            <td class="col-6 p-3 align-top"><%= book.body %></td>
          </tr>
          <% end %>
        </tbody>
      </table>


    </div>
　</div>
</div>