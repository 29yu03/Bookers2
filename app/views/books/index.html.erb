<!DOCTYPE html>
<html>
  <head>
    <script src="https://kit.fontawesome.com/0edaaf4d35.js" crossorigin="anonymous"></script>
  </head>

<div class="container px-5">
   <% if @book.errors.any? %>
  <h4 class="pt-3"><%= @book.errors.count %> errors prohibited this obj from being saved:</h4>

  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  <div class="row">
    <%= render 'list', books: @books %>


    <div class="col-md-8 offset-md-1">
      <h2>Books</h2> 画像のリンクがおかしい
      <table>
        <thead>
          <tr class="row-md-12 border-top">
            <th class="col-2 p-3"></th>
            <th class="col-4 p-3">Title</th>
            <th class="col-6 p-3">Opinion</th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
            <tr class="row-md-12 border-top">
              <td class="col-2 p-3">
                <%= link_to user_path(@user) do %>
                <%= image_tag book.user.get_profile_image(100,100) %></td>
                <% end %>
              <td class="col-4 p-3 align-top"><%= link_to book.title, book_path(book) %></td>
              <td class="col-6 p-3 align-top"><%= book.body %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    </div>
  </div>
</div>
</html>